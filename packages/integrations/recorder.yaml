recorder:
  db_url: !secret mariaDB_url
  exclude:
    domains:
      - automation
      - weblink
      - updater
      - group
      - input_boolean
      - input_number
      - input_select
      - media_player
      - weather
      - zone
    entities:
      - sun.sun
  purge_keep_days: 3
